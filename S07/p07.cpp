#include <iostream> 
using namespace std; 

int main() {
    string fullName; 
    cout << "Enter first and last name: ";
    // getline(cin, fullName);
    cin >> fullName;
    cout << fullName << endl; 
}
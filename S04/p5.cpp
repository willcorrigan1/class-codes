#include<iostream> 
using namespace std; 

int main() { 
    char ch; 
    cout << "Enter a char: "; 
    cin >> ch; 
    if ( ch == 'a') { 
        cout << "You entered the character a!\n";
    } else { 
        cout << "You didn't enter the character a!\n";
    }
}